import React, {useState} from "react";

/*
Search bar for parsing through documents in the database.
 */

const SearchBar = () => {
    const [searchContent, setSearchContent] = useState("")
}

export default SearchBar
